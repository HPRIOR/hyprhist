# Hyprhist

features

```shell

# Track history of window focus accross all monitors
hyprhist daemon --focus --monitor

# Track history of specific monitor
hyprhist daemon --focus monitor=x

# Track history of multiple monitors
hyprhist daemon --focus monitor=x monitor=y
```
